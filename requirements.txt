# Python backend dependencies — install these via:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r backend/requirements.txt   (Windows)
#   .venv/bin/python -m pip install -r backend/requirements.txt           (Linux/Mac)
#
# See backend/requirements.txt for the full list.
-r backend/requirements.txt